name = "megiay-telegram-webhook"
main = "worker.js"
compatibility_date = "2024-11-01"

# Đặt các biến công khai (không bí mật) ở đây:
[vars]
GH_OWNER = "thienpv99"
GH_REPO = "megiay"

# Các bí mật KHÔNG đặt ở file này. Đặt bằng lệnh:
#   npx wrangler secret put TELEGRAM_BOT_TOKEN
#   npx wrangler secret put TELEGRAM_SECRET
#   npx wrangler secret put GITHUB_TOKEN
